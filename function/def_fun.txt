定义一个函数要使用def语句，依次写出函数名、括号、括号中的参数和冒号:，然后在缩进块中编写函数体，函数的返回值用return语句返回

如果你已经将函数保存为.py文件了，那么，可以在文件的当前目录下自启动Python解释器，用form 文件名 import 函数名来导入函数

如果想定义一个什么事也不做的空函数，可以用pass语句:
def nop():
	pass
实际上，pass可以用来作为占位符，比如现在还没想好怎样写函数的代码，就可以先放一个pass，让代码运行起来


Python的函数返回多值其实就是返回一个tuple,但写起来更方便

